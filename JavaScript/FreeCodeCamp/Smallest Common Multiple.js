function smallestCommons(arr) {
    let sum = [];

   const t = Array(arr[1]+1).fill().filter((a,i)=>sum.push(i))

   console.log(t)
   const r = sum.reduce((acc,el)=> accel)
   console.log(r)
   return r
  }
  
  console.log(smallestCommons([1,5]));